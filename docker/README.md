# Docker Images

## Build all images

```sh
just build
```

This will create the following docker images:

- `yewstack/playground_base`
- `compiler-stable`
- `compiler-nightly`
- `clippy`
- `rustfmt`
- `cargo-expand`
